# Reachable Django backend used by the production/static build.
# Set to your LAN IP (on-site) or your public/ngrok URL so login & orders work
# when the SPA is served from GitHub Pages.
VITE_API_BASE=http://192.168.100.169:8000

# Dev/5173 build uses relative /api URLs which the Vite proxy (server.proxy in
# vite.config.js) forwards to the local Django backend on port 8000. This keeps
# dev same-origin (no CORS flakiness) and uses the real database.
# This file only affects `npm run dev` (localhost:5173), not the production build.
VITE_API_BASE=

# Dev/5173 build talks to the local Django backend on port 8000 so the
# storefront, login and admin (activity logs) work against the real database.
# This file only affects `npm run dev` (localhost:5173), not the production build.
VITE_API_BASE=http://localhost:8000
